# 单词雷达 WordRadar

把网页里的英文生词批量提取、推送到背单词 APP 的工具的领域语言。双形态:浏览器扩展(主)+ CLI(辅)。

## Language

**单词雷达 / WordRadar**:
本产品名(原名「拾词 WordPicker」,2026-08-16 更名;仓库 word-radar)。
_Avoid_: 拾词、WordPicker、wordpicker

**形态**:
面向用户的交付载体,只有两种:扩展与 CLI。
_Avoid_: 端、平台、web 版

**扩展**:
浏览器形态,唯一具备「采集网页 + 推送生词到不背单词」完整能力的形态。

**CLI**:
命令行形态,只清洗本地文件与合并词表,不推送。

**宣传页**:
v1 后置的产品营销静态站(GitHub Pages),是配套物料而非产品形态,不含任何词库功能。
_Avoid_: 官网、web 版、「静态 web 页面」(当作形态称呼时)

**采集**:
从采集目标(网页正文、上传文件)提取英文单词并做词形还原去重,得到一批候选词条。采集结果不落库,先待确认。

**采集目标**:
一次采集的输入来源。左键图标默认采集当前网页;上传文件是第一版内的另一目标;剪贴板后置。

**新词**:
本次采集的词条中,本地词库(按 lemma)没有的。是相对本地词库的判断,与不背单词生词本无关。「已在生词本」由推送阶段的查重区分,不叫新词。

**待确认批次**:
采集后、用户确认前驻留在内存的采集结果。不持久化,关闭弹窗即丢弃;重新打开弹窗对当前目标重新采集(选区不可重放,退化为全文)。

**确认**:
用户对待确认批次的放行动作。确认 = 批次合并入词库并触发一轮推送全部待推;取消 = 什么都不发生。确认即推送是唯一路径,没有自动推送开关。

**待推池**:
词库中推送标志未置位的全部词条,不区分来源(本次采集/CSV 导入/历史失败保留)。一轮推送覆盖整个待推池。

**推送**:
把待推池中的词写入不背单词生词本的动作。完成标准是「该词确认存在于生词本」——远端早已存在也算推送完成,不要求真的新增。只有成功(含远端已存在)才置推送标志;失败保持待推。

**已存在**:
推送过程中查重发现该词已在不背单词**生词本**(远端)的状态。判断主体是远端生词本,不是本地词库。与新词是一对易混词:新词看本地词库,已存在看远端;一个词可以既是新词,又在推送时被判为已存在。
_Avoid_: 用「已存在」描述本地词库中已有词条(那叫非新词)
